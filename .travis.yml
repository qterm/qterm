language: cpp

compiler:
 - gcc

before_install:
 - sudo add-apt-repository --yes ppa:ubuntu-sdk-team/ppa
 - sudo apt-get update -qq
 - sudo apt-get install qt5-default qttools5-dev-tools qtbase5-dev qttools5-dev

script:
 - mkdir build
 - cd build
 - cmake .. -DQT5=Yes
 - make
