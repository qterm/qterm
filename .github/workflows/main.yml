name: Build

on:
  - pull_request
  - push

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: true
      matrix:
        os:
          - ubuntu-20.04
          - macos-13
          - windows-2019
        qt:
          - version: '5.9.9'
            modules: 'qtscript'
          - version: '6.2.3'
            modules: 'qt5compat'
          - version: '6.7.2'
            modules: 'qt5compat'
            release: true
    steps:
      - uses: actions/checkout@v4
      - name: Install Dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt update
          sudo apt -y install cmake
      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ matrix.qt.version }}
          dir: ${{ github.workspace }}/qt
          modules: ${{ matrix.qt.modules }}
          cache: true
      - name: Build
        run: |
          cmake -B build
          cmake --build build --config Release
      - name: Package
        if: matrix.qt.release == true && runner.os != 'Linux'
        run: |
          cd build
          cpack
      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}-Package
          path: |
            build/QTerm-*.exe
            build/QTerm-*.dmg
  release:
    needs: build
    runs-on: ubuntu-20.04
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true
      - name: Create Release
        run: |
          gh release create ${{ github.ref_name }} --draft dist/QTerm-*.exe dist/QTerm-*.dmg
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
